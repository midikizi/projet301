<form method="post" action="{{ path('app_tontines_delete', {'id': tontine.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tontine.id) }}">
    <button class="btn">Delete</button>
</form>
